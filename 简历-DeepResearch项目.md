# 项目经验

## DeepResearch —— 多智能体协同深度研究系统

**独立开发（全栈）** | 2025 | Python ~2500 行 + 前端 ~1000 行

用户输入任意问题，系统自主完成「意图路由 → 搜索规划 → 网页取证 → 证据评分 → 报告撰写」全流程，输出带可信度评分的 Markdown 研究报告。支持 CLI / Web / Docker 部署。

---

### 架构与选型理由

```
意图路由 ──简单问题──▶ 直接回答
  │
  │ 复杂问题
  ▼
搜索规划 ──▶ 网页侦察 ──▶ 证据分析 ──▶ 报告撰写
                ▲            │
                │  证据不足回环  │
                └──────────────┘
```

| 环节 | 选型 | 为什么 |
|------|------|--------|
| Agent 编排 | **LangGraph StateGraph** | 需要条件路由（简单/复杂分流）+ 循环回环（证据不足重搜），纯 Chain 无法表达分支和循环；自建状态机成本高 |
| LLM 对话 | **LangChain create_agent** | 封装了工具调用循环，Web Scout 和 Analyst 需要自主决定何时调用搜索/抓取工具，手写 ReAct 循环必要性不大 |
| 记忆存储 | **SQLite** | 零运维的嵌入式数据库，单用户/小团队场景不需要 PostgreSQL；同时复用做 LangGraph 检查点存储，不引入额外依赖 |
| 搜索引擎 | **Bocha + DuckDuckGo + DeepSeek 三层** | Bocha 中文强但付费且偶有结果不足；DuckDuckGo 免费但中文弱且无新鲜度过滤；DeepSeek web_search 补充但要省 Token——三者互补，自动降级 |
| Web 框架 | **FastAPI + SSE** | 研究是长任务（30s-2min），不能用普通 HTTP 让用户干等；WebSocket 过重，SSE 单向推送刚好匹配逐节点进度汇报的需求 |
| 前端 | **零框架 HTML/CSS/JS** | 只有两个页面（首页 + 对话），React/Vue 的构建链和运行时体积得不偿失；手写 Markdown 渲染器 + SSE 解析器总共不到 500 行 |

---

### 核心技术实现

**三维证据可信度模型：** 自研加权评分 `可信度 = 相关性×0.4 + 时效性×0.3 + 权威性×0.3`，低分证据硬过滤（<0.4 丢弃），不足时自动触发回环重搜。解决 LLM 容易"一本正经引用垃圾来源"的幻觉问题。

**跨会话记忆：** LLM 自动压缩长应答（>500 字 → ≤300 字保留关键数据），关键词规则提取用户画像（职业/学历/地点），每次请求注入画像 + 最近 8 条对话到所有 Agent Prompt。解决每次对话"像第一次见面"的上下文断裂问题。

**多级降级策略：** 搜索 API 三源自动切换、网页抓取 LLM 失败后回退直接抓取、工具消息提取兜底。解决外部依赖不可控的鲁棒性问题。

**成本控制：** 意图预检测 40+ 关键词匹配命中直接回答则跳过 LLM 调用；补充搜索仅前 4 个查询调用深层 API；用户画像注入减少重复澄清的 Token 消耗。

---

### 技术栈

`Python` `LangGraph` `LangChain` `FastAPI` `SQLite` `SSE` `Docker` `DeepSeek API` `DuckDuckGo` `pydantic-settings`
